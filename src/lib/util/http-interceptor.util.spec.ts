import { HTTPInterceptor } from './http-interceptor.util';

describe('HTTPInterceptor', () => {
  it('should pass', () => {
    expect(HTTPInterceptor).toBeDefined();
  });

  it.todo('HttpInterceptor 구현체 리스트를 해당하는 hooks 배열에 추가한다.');
  it.todo('HttpInterceptor 인터페이스에 해당하지 않는 hooks는 추가하지 않는다');
});
